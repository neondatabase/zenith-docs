# Unsupported/changed PostgreSQL commands

// im thinking we should use this section capture all ways in which Zenith functions differently than PostgreSQL (in a way that an end user would care about).

